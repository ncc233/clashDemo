<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Clash.UI.Suppot.UI.Controls">
    <BorderGapMaskConverter x:Key="BorderGapMaskConverter"/>


    
    
    <Style x:Key="headerTextBoxStyle" TargetType="{x:Type controls:HeaderTextBox}">
        <Setter Property="Height" Value="40" />
        <Setter Property="Width" Value="400" />
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="BorderThickness" Value="1 1 1 1" />
        <Setter Property="BorderBrush" Value="{DynamicResource hComboxBoxBorderBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:HeaderTextBox}">
                    <Grid SnapsToDevicePixels="true" Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="6"/>
                        </Grid.RowDefinitions>
                        <Border x:Name="boardBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Grid.ColumnSpan="4" Grid.RowSpan="3" Grid.Row="1">
                            <Border.OpacityMask>
                                <MultiBinding ConverterParameter="7" Converter="{StaticResource BorderGapMaskConverter}">
                                    <Binding ElementName="Header" Path="ActualWidth"/>
                                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource Mode=Self}"/>
                                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource Mode=Self}"/>
                                </MultiBinding>
                            </Border.OpacityMask>
                            <Border BorderBrush="Transparent" BorderThickness="4" CornerRadius="3">
                                <Border BorderBrush="Transparent"  BorderThickness="5" CornerRadius="2"/>
                            </Border>
                        </Border>
                        <Border  x:Name="Header" Grid.Column="1" Padding="0" Background="Transparent" Grid.RowSpan="2" Grid.Row="0">
                            <!--<TextBlock Text="{Binding ElementName=cText,Path=Text}"/>-->
                        </Border>
                        <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="3" Margin="3">
                            <TextBlock Name="moveText" Text="{TemplateBinding Header}" Foreground=" #c4c4c4" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left" RenderTransformOrigin="0 0">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                            <ScrollViewer FontSize="{TemplateBinding FontSize}" x:Name="PART_ContentHost" Focusable="false" VerticalAlignment="Center" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource hComboxBoxMouseOverBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <!--<Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource Storyboard1}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource Storyboard2}"/>
                            </Trigger.ExitActions>-->
                            <Setter Property="BorderBrush" Value="{DynamicResource jumpButtonForegroundBrush}"/>
                            <Setter TargetName="moveText" Property="Foreground" Value="{DynamicResource jumpButtonForegroundBrush}"/>
                            <Setter Property="BorderThickness" Value="1.2 1.2 1.2 1.2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>