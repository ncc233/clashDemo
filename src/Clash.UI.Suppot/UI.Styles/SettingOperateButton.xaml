<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:Clash.UI.Suppot.UI.Helpers"
                    xmlns:controls="clr-namespace:Clash.UI.Suppot.UI.Controls">
    <Style x:Key="settingOperateButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="{DynamicResource jumpButtonForegroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource jumpButtonForegroundBrush}"/>
        <Setter Property="helpers:RippleAnimationHelper.IsEnable" Value="True"/>
        <Setter Property="helpers:RippleAnimationHelper.RippleParentRadius" Value="4"/>
        <Setter Property="helpers:RippleAnimationHelper.RippleBrush" Value="{DynamicResource jumpButtonForegroundBrush}"/>
        <Setter Property="FontSize" Value="17"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="superButtonTgrid">
                        <Border Name="backBorder" 
                                Opacity="0"
                                CornerRadius="4"
                            Background="{TemplateBinding Background}"
                                />
                        <TextBlock VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                   Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"  Value="True">
                            <Setter TargetName="backBorder" Property="Opacity" Value="0.1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>