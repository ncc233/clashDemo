<Window x:Class="ClashDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClashDemo"
        xmlns:controls="clr-namespace:Clash.UI.Suppot.UI.Controls;assembly=Clash.UI.Suppot"
        xmlns:helpers="clr-namespace:Clash.UI.Suppot.UI.Helpers;assembly=Clash.UI.Suppot"
        xmlns:vm="clr-namespace:ClashDemo.ViewModels"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Icon="/Icons/icon.ico"
        Title="Clash Verge" Height="736" Width="953.6"
        MinWidth="953.6"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <Window.Resources>


    </Window.Resources>
    <Grid x:Name="gridMain" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="207"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--右侧内容区域-->

        <Frame Grid.Column="1" Background="{DynamicResource contentBackgroundBrush}" 
               helpers:PageNavigationHelper.NavigateTarget="{Binding ElementName=navigationBar,
            Path=SelectedItem,Converter={StaticResource navigateConvert},Mode=TwoWay}"
               Navigated="Frame_Navigated"
               NavigationUIVisibility="Hidden">
            <Frame.Effect>
                <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="1"/>
            </Frame.Effect>
        </Frame>
        <!--导航栏区-->
        <Grid Background="{DynamicResource commonBarBrush}" >
            <Grid.Effect>
                <DropShadowEffect BlurRadius="3" ShadowDepth="0" Direction="0" Opacity="0.2"/>
            </Grid.Effect>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Image Margin="0 15 0 15" Source="/Icons/tray-icon-mono.ico"/>
                <TextBlock Text="Clash Verge" 
                           TextAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="23"
                           FontFamily="Bahnschrift" 
                           FontWeight="Bold"/>
            </StackPanel>
            <ListBox Grid.Row="1" x:Name="navigationBar" Style="{StaticResource navigationBar}"  ItemsSource="{Binding NavigationItems}">
            </ListBox>
            <Grid Grid.Row="2">
                <!--<controls:PageJumpButton Style="{StaticResource jumpButtonStyle}" Content="订 阅" Tag="{StaticResource subscribeGeometry}"/>-->
                <!--<CheckBox  Content="12122" VerticalAlignment="Center" HorizontalAlignment="Left"/>-->



            </Grid>
        </Grid>
    </Grid>
</Window>
