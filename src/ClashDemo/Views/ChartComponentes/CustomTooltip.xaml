<UserControl x:Class="ClashDemo.Views.ChartComponentes.CustomTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClashDemo.Views.ChartComponentes"
             xmlns:lv="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ItemsControl ItemsSource="{Binding Data.Points}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type lv:DataPointViewModel}">
                    <Border BorderBrush="#DDD" BorderThickness="0,0,0,0" 
                    Margin="0,0,0,0" Padding="8" >
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5" VerticalAlignment="Center">
                                <Ellipse Width="15" Height="15" 
                                 Fill="{Binding Series.Stroke}" 
                                 Stroke="White" StrokeThickness="1"
                                 VerticalAlignment="Center" Margin="0,0,8,0"/>

                                <TextBlock Text="{Binding Series.Title}" 
                                   FontWeight="Bold" 
                                   FontSize="14" 
                                   Foreground="#333333"
                                   VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid Margin="20,0,5,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="报警信息：" Foreground="Gray"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ChartPoint.Instance.AlarmMsg}" Margin="5,0,0,0"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="数值：" Foreground="Gray"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ChartPoint.Instance.Value}" FontWeight="Bold" Margin="5,0,0,0"/>
                            </Grid>

                        </StackPanel>
                    </Border>
                </DataTemplate>

            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
